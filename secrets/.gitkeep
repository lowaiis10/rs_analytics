# This file ensures the secrets/ directory exists in git
# while keeping actual secrets out (via .gitignore)
#
# SECURITY INSTRUCTIONS:
# 1. Place your service account JSON files in this directory
# 2. Ensure files have restricted permissions: chmod 600 *.json
# 3. NEVER commit actual credential files to git
#
# Expected files:
# - ga4_service_account.json  (Google Analytics 4 service account)
# - bq_service_account.json   (BigQuery service account, if using BQ mirror)
